{% comment %}
UltraFresh Size Guide Section
- Simple, plain style inspired by "nutritional info" look
- Two-column table like your first image
- Fully self-contained (no snippets)
{% endcomment %}

<section class="uf-size-guide page-width">
  <div class="sg-card" style="--sg-radius:{{ section.settings.radius }}px">
    {% if section.settings.collapsible %}
      <details class="sg-accordion" {% if section.settings.open_by_default %}open{% endif %}>
        <summary class="sg-summary">
          <span class="sg-summary-title">
            {% if section.settings.header_icon != blank %}
              <span class="sg-icon" aria-hidden="true">{{ section.settings.header_icon }}</span>
            {% endif %}
            {{ section.settings.title }}
          </span>
          {% if section.settings.subtitle != blank %}
            <span class="sg-subtitle">{{ section.settings.subtitle }}</span>
          {% endif %}
        </summary>
        <div class="sg-content">
          <div class="sg-table-wrap">
            <table class="sg-table">
              <thead>
                <tr>
                  <th></th>
                  <th>{{ section.settings.col_a_title }}</th>
                  <th>{{ section.settings.col_b_title }}</th>
                </tr>
              </thead>
              <tbody>
                {% for block in section.blocks %}
                  <tr id="sg-row-{{ block.id }}">
                    <th scope="row" class="sg-label">
                      {% if block.settings.icon != blank %}
                        <span class="sg-emoji" aria-hidden="true">{{ block.settings.icon }}</span>
                      {% endif %}
                      {{ block.settings.label }}
                    </th>
                    <td>{{ block.settings.col_a }}</td>
                    <td>{{ block.settings.col_b }}</td>
                  </tr>
                {% endfor %}
              </tbody>
            </table>
          </div>
        </div>
      </details>
    {% else %}
      <div class="sg-summary sg-summary--static">
        <span class="sg-summary-title">
          {% if section.settings.header_icon != blank %}
            <span class="sg-icon" aria-hidden="true">{{ section.settings.header_icon }}</span>
          {% endif %}
          {{ section.settings.title }}
        </span>
        {% if section.settings.subtitle != blank %}
          <span class="sg-subtitle">{{ section.settings.subtitle }}</span>
        {% endif %}
      </div>
      <div class="sg-content">
        <div class="sg-table-wrap">
          <table class="sg-table">
            <thead>
              <tr>
                <th></th>
                <th>{{ section.settings.col_a_title }}</th>
                <th>{{ section.settings.col_b_title }}</th>
              </tr>
            </thead>
            <tbody>
              {% for block in section.blocks %}
                <tr id="sg-row-{{ block.id }}">
                  <th scope="row" class="sg-label">
                    {% if block.settings.icon != blank %}
                      <span class="sg-emoji" aria-hidden="true">{{ block.settings.icon }}</span>
                    {% endif %}
                    {{ block.settings.label }}
                  </th>
                  <td>{{ block.settings.col_a }}</td>
                  <td>{{ block.settings.col_b }}</td>
                </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Size Guide (Plain Table)",
  "tag": "section",
  "class": "uf-size-guide-section",
  "settings": [
    { "type": "text", "id": "title", "label": "Header title", "default": "Size Guide" },
    { "type": "text", "id": "subtitle", "label": "Header subtitle", "default": "Compare styles and sizes at a glance" },
    { "type": "text", "id": "header_icon", "label": "Header icon (emoji/text)", "default": "📏" },

    { "type": "text", "id": "col_a_title", "label": "Left column title", "default": "TAPED DIAPER" },
    { "type": "text", "id": "col_b_title", "label": "Right column title", "default": "PULL-UP DIAPER" },

    { "type": "checkbox", "id": "collapsible", "label": "Make it collapsible", "default": true },
    { "type": "checkbox", "id": "open_by_default", "label": "Open by default", "default": true },
    { "type": "range", "id": "radius", "min": 0, "max": 16, "step": 2, "unit": "px", "label": "Card corner radius", "default": 12 }
  ],
  "blocks": [
    {
      "type": "row",
      "name": "Row",
      "settings": [
        { "type": "text", "id": "icon", "label": "Row icon (emoji/text)", "default": " " },
        { "type": "text", "id": "label", "label": "Row label", "default": "Best for" },
        { "type": "text", "id": "col_a", "label": "Left value", "default": "Newborn & infants" },
        { "type": "text", "id": "col_b", "label": "Right value", "default": "Active toddlers & walkers" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Size Guide (Plain Table)",
      "blocks": [
        { "type": "row", "settings": { "icon": "", "label": "Best for", "col_a": "Newborn & infants", "col_b": "Active toddlers & walkers" } },
        { "type": "row", "settings": { "icon": "🛏️", "label": "Changing style", "col_a": "Lying down (easy for small babies)", "col_b": "Standing up or on-the-go changes" } },
        { "type": "row", "settings": { "icon": "🧼", "label": "Freedom to move", "col_a": "Secure side tapes for adjustability", "col_b": "360° stretchy waistband for comfort" } },
        { "type": "row", "settings": { "icon": "🌙", "label": "Night protection", "col_a": "Soft and snug fit", "col_b": "Easy to wear, perfect for active play" } },
        { "type": "row", "settings": { "icon": "🎯", "label": "Sizes available", "col_a": "S, M, L, XL", "col_b": "L, XL, XXL, XXXL" } },
        { "type": "row", "settings": { "icon": "⏱️", "label": "Night-time protection", "col_a": "XL for overnight", "col_b": "XXL & XXXL for all-night dryness" } },
        { "type": "row", "settings": { "icon": "👍", "label": "Great for", "col_a": "Ideal for first-time moms", "col_b": "Potty training, travel, busy toddlers" } }
      ]
    }
  ]
}
{% endschema %}

<style>
.uf-size-guide .sg-card{
  background:#fff;
  border:1px solid #e5e7eb;
  border-radius:var(--sg-radius,12px);
  overflow:hidden;
  font-family:inherit;
}

/* Summary (header) */
.uf-size-guide .sg-summary{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:1rem 1.25rem;
  cursor:pointer;
  background:#f7f7f9;
  border-bottom:1px solid #e5e7eb;
  gap:.5rem;
}
.uf-size-guide .sg-summary--static{ cursor:default; }
.uf-size-guide details > summary { list-style:none; }
.uf-size-guide details > summary::-webkit-details-marker{ display:none; }

.uf-size-guide .sg-summary-title{
  display:flex; align-items:center; gap:.5rem;
  font-weight:600; color:#111827;
}
.uf-size-guide .sg-icon{ font-size:1.1rem; }
.uf-size-guide .sg-subtitle{
  color:#6b7280; font-size:.9rem;
}

/* caret */
.uf-size-guide .sg-summary:after{
  content:"▾";
  margin-left:.75rem;
  transition:transform .2s ease;
  font-size:.9rem; color:#6b7280;
}
.uf-size-guide details[open] .sg-summary:after{ transform:rotate(180deg); }

/* Table */
.uf-size-guide .sg-table-wrap{ overflow-x:auto; }
.uf-size-guide table.sg-table{
  width:100%;
  border-collapse:collapse;
  table-layout:fixed;
  font-size:.95rem;
}
.uf-size-guide .sg-table th,
.uf-size-guide .sg-table td{
  padding:.9rem 1rem;
  vertical-align:top;
  border-bottom:1px solid #e5e7eb;
  color:#111827;
}
.uf-size-guide .sg-table thead th{
  font-weight:600;
  text-align:left;
  white-space:nowrap;
  background:#f9fafb;
}
.uf-size-guide .sg-label{
  width:34%;
  color:#374151;
  font-weight:500;
}
.uf-size-guide .sg-emoji{
  margin-right:.35rem;
}
@media (max-width: 768px){
  .uf-size-guide .sg-label{ width:40%; }
  .uf-size-guide .sg-table th, .uf-size-guide .sg-table td{ padding:.75rem .75rem; }
}
</style>
